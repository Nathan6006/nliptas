name = "nlimtas"
compatibility_date = "2026-09-23"
pages_build_output_dir = "public"

# KV namespace for the shared ledger. The id is an account-scoped
# identifier, not a secret; the passphrase lives in the LEDGER_KEY secret.
[[kv_namespaces]]
binding = "LEDGER"
id = "19744ee724e144a99a0e0dfe0b0624f6"
