name = "nlimtas"
compatibility_date = "2026-09-23"
pages_build_output_dir = "public"

# KV namespace for the shared ledger. The id is filled in by
#   npx wrangler kv namespace create LEDGER
# It is an account-scoped identifier, not a secret.
# [[kv_namespaces]]
# binding = "LEDGER"
# id = "<namespace id>"
